1. None means doesn't exist in our system, False is boolean expresion some object or condition can be False but still exist
   and 0 is integer ValueError
   
2. 2,4,6 it doubles the values for all list elements
3. [lambda x: x ** 2 for x in numbers] lambda is unkown function
4. pass
5. 
    nums = [3, 5, 7, 9, 11]
    def odd_numbers(arr)
        odd = []
        for num in arr:
            if num % 2 != 0:
                odd.append(num)
        return odd
6. print(my_list[-1]) #not sure what intention were with this
7.1,4,7,10
8.  def is_palindrome(str):
        new_str = str[::-1]
        if str == new_str:
            return True
        else:
            return False
9. Lists, boolean, 
10. syntax error won't run due to interpretation and with runtime error the program ran just hit the problem or something went wrong
11. probably referencing the variable, try to use different SyntaxError
12. loop is the function that repeat itself until some rules are met, and condition is some set of rules